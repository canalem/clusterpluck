[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "clusterpluck"
authors = [{name = "Marc Canale", email = "marc.canale@hotmail.com"}]
dynamic = ["version", "description"]